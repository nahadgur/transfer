# Handoff queue

## 2026-06-15 01:31 — transfer-writer (transfer / Kenya's Transfer Hub)
- Sites: transfer — wrote one H9 pension-transfer spoke (draft): `what-happens-to-your-pension-when-you-leave-a-job-kenya` (id 115). YMYL-financial, sourced to RBA, NSSF and the Finance Act 2025.
- Staged files: lib/blog-transfer-4c.ts (new post appended); docs/SILO-PLAN.md (section 6 tracker line). HANDOFF-QUEUE.md is not committed per spec.
- Live flips this run: none (writer only; spoke is draft:true)
- tsc: pass (npx tsc --noEmit exit 0 after rename round-trip; link gate 0 broken)
- Commit prompt / git state: BLOCKED. The two files are staged (git status: `M docs/SILO-PLAN.md`, `M lib/blog-transfer-4c.ts`) but commit fails: `.git/index.lock` exists and cannot be removed from the sandbox ("Operation not permitted"), and `git add` reports `index file corrupt` / `bad signature 0x00000000`. safe-commit.sh could not recover it. Same blocker as the 2026-06-13 H7/H8 runs. Needs Claude Code (native, not sandbox) to delete `.git/index.lock`, repair/reset the index (`git reset` to rebuild from HEAD), re-add lib/blog-transfer-4c.ts + docs/SILO-PLAN.md, commit and push. The H8 land-search draft and this H9 draft are both uncommitted in the working tree.
- Status: PENDING PUSH (commit blocked by corrupt index + stale lock; working tree changes are intact)

## 2026-06-15 02:12 — transfer-writer (transfer / Kenya's Transfer Hub)
- Sites: transfer — wrote one H10 import-duty spoke (draft): `import-duty-on-phones-and-electronics-kenya` (id 116). YMYL-financial, sourced to KRA / National Treasury (phone import stack 16% VAT + 10% excise + 25% import duty + 2.5% IDF + 2% RDL, ~55.5% aggregate; RDL 2% since end-2024; many laptops 0% duty; Finance Bill 2026 25%-at-activation excise flagged as a proposal, not law). Up-link /guides/import-duty/ + pillar /import-duty/, one external (kra.go.ke), verify-with-KRA note. All ten hubs now seeded; round-robin restarts at H1 next run.
- Staged files: lib/blog-transfer-4c.ts (new post appended, id 116); docs/SILO-PLAN.md (section 6 tracker line). HANDOFF-QUEUE.md not committed per spec.
- Live flips this run: none (writer only; spoke is draft:true)
- tsc: pass (npx tsc --noEmit exit 0 after rename round-trip; link gate 0 broken)
- Commit prompt / git state: BLOCKED, worse than prior runs. `.git/index.lock` (0-byte, Jun 15 01:31) still present AND `.git/index` is now corrupt ("bad signature 0x00000000 / index file corrupt"); `git status` itself fails. Sandbox `rm` returns "Operation not permitted" on both .git files, so safe-commit.sh cannot recover. Needs Claude Code (native): remove `.git/index.lock`, remove the corrupt `.git/index`, run `git reset` to rebuild the index from HEAD (non-destructive to working tree), then add lib/blog-transfer-4c.ts + docs/SILO-PLAN.md, commit and push. NOTE: the uncommitted backlog likely now spans the H7/H8/H9 drafts plus this H10 one — all live in the working tree, none committed. Worth `git log`-checking which of ids 113-116 actually reached main before committing.
- Status: PENDING PUSH (commit blocked by corrupt index + stale lock; working tree changes are intact)

## 2026-06-15 03:10 — transfer-publisher (transfer)
- Sites: transfer — PUBLISHER run. Flipped the oldest parked draft live: H5 mpesa-how-tos `how-to-get-full-mpesa-statement` (id 112) set draft:false, publishedAt + lastReviewedAt = 2026-06-15. Flip-time links already present (inline up-link /guides/mpesa-how-tos/ + tool-pillar /ussd-codes/); no live H5 sibling exists, so hub-only, no /blog/ sibling links and no reciprocal wiring. QC pass: Kenya English, no reserved head terms, Safaricom figures attributed + "verify on official page" note, no em dashes, schema fields (faqs + publish/review dates) intact.
- Staged files: lib/blog-transfer-4c.ts (id 112 flipped to draft:false + dates); docs/SILO-PLAN.md (section 6 publisher log line). HANDOFF-QUEUE.md not committed per spec.
- Live flips this run: how-to-get-full-mpesa-statement (id 112) — flipped in the WORKING TREE only; NOT actually deployed until git is repaired and pushed (see below).
- tsc: pass (npx tsc --noEmit exit 0 after rename round-trip; link gate 0 broken across 10 slugs)
- Commit prompt / git state: STILL BLOCKED, same as prior runs. `.git/index.lock` (0-byte, Jun 15 01:31) present AND `.git/index` corrupt ("bad signature 0x00000000 / index file corrupt"); `git status` fails; safe-commit.sh cannot proceed. Sandbox `rm` returns "Operation not permitted" on both .git files. Needs Claude Code (native, outside sandbox): remove `.git/index.lock`, remove corrupt `.git/index`, `git reset` to rebuild index from HEAD (non-destructive to working tree), then add lib/blog-transfer-4c.ts + docs/SILO-PLAN.md, commit and push. The uncommitted backlog now spans H5 flip (id 112) + H7/H8/H9/H10 drafts (ids 113-116) — all in the working tree. `git log` ids 112-116 against main before committing to see what already landed. Until this is drained, NO publisher flip can reach the live site.
- Status: PENDING PUSH (commit blocked by corrupt index + stale lock; working-tree changes intact)

## 2026-06-15 04:30 — transfer-writer (transfer / Kenya's Transfer Hub)
- Sites: transfer — wrote one H1 mpesa-charges spoke (draft): `how-much-fuliza-costs-in-kenya` (id 117). YMYL-financial, sourced to the official Safaricom Fuliza page (last updated 23 Jun 2025): 1% one-off access fee, daily maintenance fee bands incl 20% excise, free 3-day window on amounts KSh 1,000 and below, limit up to KSh 70,000. Distinct intent (Fuliza overdraft cost), not the reserved M-Pesa charges head term. Up-link /guides/mpesa-charges/ + pillar /mpesa-calculator/; one external (safaricom.co.ke, nofollow); rate-sensitive verify note; relatedSlugs = 2 live H1 siblings.
- Staged files: lib/blog-transfer-4c.ts (new post appended); docs/SILO-PLAN.md (section 6 tracker line). HANDOFF-QUEUE.md is not committed per spec.
- Live flips this run: none (writer)
- tsc: pass (npx tsc --noEmit exit 0 after rename round-trip)
- Commit prompt / git state: COMMIT BLOCKED. .git/index.lock (0-byte, Jun 15 01:31) present AND .git/index corrupt ("bad signature 0x00000000 / index file corrupt"). Sandbox `rm` returns "Operation not permitted" on both; `git reset` fails on the corrupt index. Needs Claude Code on the host to: remove .git/index.lock + the corrupt .git/index, run `git reset` to rebuild the index from HEAD (non-destructive to the working tree), then commit + push. This will drain the accumulated backlog: H5 publisher flip + H7/H8/H9/H10 drafts + this H1 draft, plus the SILO-PLAN tracker edits.
- Status: PENDING PUSH

## 2026-06-16 10:14 — transfer-writer (transfer / Kenya's Transfer Hub)
- Sites: transfer — wrote one H2 bank-transfers spoke (draft): `how-pesalink-works-and-its-limits-kenya` (id 118). YMYL-financial, sourced to IPSL/PesaLink (operator owned by the Kenya Bankers Association): min KSh 10, max KSh 999,999 per transaction; instant 24/7 account-to-account; 100+ connected institutions; daily limits set per bank; and Business Daily for the 2025 discounted tariffs (KCB free below KSh 1,000, flat KSh 20 above). Distinct intent vs the live H2 spoke rtgs-vs-mpesa-large-amounts. Up-link /guides/bank-transfers/ + pillar /bank-transfer/; one inline sibling link to live rtgs-vs-mpesa-large-amounts (/blog/); two externals (pesalink.co.ke + businessdailyafrica.com, one per domain, nofollow); rate-sensitive "limits and fees set by your bank, verify" note included. Next round-robin pick: H6 kplc-tokens.
- Staged files: lib/blog-transfer-4c.ts (new post appended, id 118); docs/SILO-PLAN.md (section 6 tracker line). HANDOFF-QUEUE.md not committed per spec.
- Live flips this run: none (writer only; spoke is draft:true)
- tsc: pass (npx tsc --noEmit exit 0 after rename round-trip; link gate 0 broken across 10 slugs)
- Commit prompt / git state: BLOCKED by the stale `.git/index.lock` (0-byte, Jun 15 01:31). GOOD NEWS this run: `.git/index` is NO LONGER corrupt — `git status` and `git log` both work, so no index rebuild is needed, only the lock must go. Sandbox `rm`/`mv` on `.git/index.lock` still returns "Operation not permitted"; safe-commit.sh hangs on the lock (timed out). Needs Claude Code (native): remove `.git/index.lock`, then `git add lib/blog-transfer-4c.ts docs/SILO-PLAN.md && git commit && git push`. HEAD is at 397f1d8 (H7 logbook draft) + b82a066 (H5 statement); the uncommitted working-tree backlog to drain = H5 publisher flip (id 112), H8/H9/H10 drafts (ids 114-116), H1 Fuliza draft (id 117) and this H2 PesaLink draft (id 118), plus the accumulated SILO-PLAN tracker edits. `git log` ids 112-118 against main before committing to confirm exactly what already landed.
- Status: PENDING PUSH (commit blocked by stale lock; working-tree changes intact)
